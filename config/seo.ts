/**
 * SEO config – keywords and meta used in <head> for all pages.
 * Extend with openGraph, twitter, etc. as needed.
 */

export const seo = {
  /** Meta keywords (comma-separated or array; included in <meta name="keywords">). */
  keywords: [
    "hiking",
    "trail running",
    "ultralight",
    "outdoor",
    "travel",
    "long-distance hiking",
    "backpacking",
    "adventure",
  ],
  /** Default meta description (used when page doesn't override). */
  defaultDescription:
    "A blog about hiking adventures, trail running, and outdoor exploration.",
  /** Default title template (used when page doesn't override). */
  defaultTitle: "Lindekaer",
};
